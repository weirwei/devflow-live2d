{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for DPartner.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
